import { PriceHighlight } from './price-highlight';

describe('PriceHighlight', () => {
  it('should create an instance', () => {
    const directive = new PriceHighlight();
    expect(directive).toBeTruthy();
  });
});
