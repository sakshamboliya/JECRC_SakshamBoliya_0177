import { ClickBlock } from './click-block';

describe('ClickBlock', () => {
  it('should create an instance', () => {
    const directive = new ClickBlock();
    expect(directive).toBeTruthy();
  });
});
